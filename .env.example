# Usually blank. The browser derives the bridge hostname and uses ports below.
NEXT_PUBLIC_WS_URL=
NEXT_PUBLIC_API_URL=

WS_PORT=8080
HTTP_PORT=8081

DISABLE_PC_ACTIONS=false
ALLOW_ARBITRARY_BUILD_URLS=false

# Optional overrides for Quest HTTPS/WSS mode.
TLS_CERT_PATH=
TLS_KEY_PATH=
